from panda3d.core import (
    NodePath,
)


class generation:
    def __init__() -> None:
        # setup vars
        system = {
            "planets": None,
            "features": None,
            "": None,
        }

        # TBD

        return system


class build:
    def __init__(world) -> None:
        rootNode = None
        itemDict = world

        # TBD

        return {"rootNode": rootNode, "itemDict": itemDict}
